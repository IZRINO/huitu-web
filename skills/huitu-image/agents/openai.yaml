interface:
  display_name: "Huitu Image Generation"
  short_description: "Queue image generation and edits with automatic downloads"
  default_prompt: "Use $huitu-image to generate images from my prompts, reuse my model profile, submit to the shared queue, and return the downloaded files."
